{
  "name": "receptionist-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm run build",
    "dev": "concurrently --kill-others --names \"ng,node\" \"cd frontend && npx ng build --watch --configuration development\" \"node --watch server.js\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.22.4"
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
